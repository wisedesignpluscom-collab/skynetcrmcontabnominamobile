{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "rm -f package-lock.json && npm install --no-audit --no-fund",
  "buildCommand": "npm run build:vercel"
}
